/*----------------------LEVEL THREE GRID-----------------------*/

/*---------LEVEL THREE FUNCTIONS START HERE------------*/
//isolate first box in level three
function levelThreeGridBox1(j) {
  let box1 = 13;
  let across = 2;
  let nextLine = 12;
  if (
    (j > box1 && j < box1 + across) ||
    (j > box1 + nextLine && j < box1 + nextLine + across) ||
    (j > box1 + 2 * nextLine && j < box1 + 2 * nextLine + across) ||
    (j > box1 + 3 * nextLine && j < box1 + 3 * nextLine + across)
  ) {
    return true;
  } else {
    return false;
  }
}

//isolate second box in level three
function levelThreeGridBox2(j) {
  let box2 = 14;
  let across = 2;
  let nextLine = 12;
  if (
    (j > box2 && j < box2 + across) ||
    (j > box2 + nextLine && j < box2 + nextLine + across) ||
    (j > box2 + 2 * nextLine && j < box2 + 2 * nextLine + across) ||
    (j > box2 + 3 * nextLine && j < box2 + 3 * nextLine + across)
  ) {
    return true;
  } else {
    return false;
  }
}

//isolate third box in level three
function levelThreeGridBox3(j) {
  let box3 = 15;
  let across = 2;
  let nextLine = 12;
  if (
    (j > box3 && j < box3 + across) ||
    (j > box3 + nextLine && j < box3 + nextLine + across) ||
    (j > box3 + 2 * nextLine && j < box3 + 2 * nextLine + across) ||
    (j > box3 + 3 * nextLine && j < box3 + 3 * nextLine + across)
  ) {
    return true;
  } else {
    return false;
  }
}

//isolate fourth box in level three
function levelThreeGridBox4(j) {
  let box4 = 16;
  let across = 2;
  let nextLine = 12;
  if (
    (j > box4 && j < box4 + across) ||
    (j > box4 + nextLine && j < box4 + nextLine + across) ||
    (j > box4 + 2 * nextLine && j < box4 + 2 * nextLine + across) ||
    (j > box4 + 3 * nextLine && j < box4 + 3 * nextLine + across)
  ) {
    return true;
  } else {
    return false;
  }
}

//isolate fifth box in level three
function levelThreeGridBox5(j) {
  let box5 = 17;
  let across = 2;
  let nextLine = 12;
  if (
    (j > box5 && j < box5 + across) ||
    (j > box5 + nextLine && j < box5 + nextLine + across) ||
    (j > box5 + 2 * nextLine && j < box5 + 2 * nextLine + across) ||
    (j > box5 + 3 * nextLine && j < box5 + 3 * nextLine + across)
  ) {
    return true;
  } else {
    return false;
  }
}

//isolate sixth box in level three
function levelThreeGridBox6(j) {
  let box6 = 18;
  let across = 2;
  let nextLine = 12;
  if (
    (j > box6 && j < box6 + across) ||
    (j > box6 + nextLine && j < box6 + nextLine + across) ||
    (j > box6 + 2 * nextLine && j < box6 + 2 * nextLine + across) ||
    (j > box6 + 3 * nextLine && j < box6 + 3 * nextLine + across)
  ) {
    return true;
  } else {
    return false;
  }
}

//isolate seventh box in level three
function levelThreeGridBox7(j) {
  let box7 = 19;
  let across = 2;
  let nextLine = 12;
  if (
    (j > box7 && j < box7 + across) ||
    (j > box7 + nextLine && j < box7 + nextLine + across) ||
    (j > box7 + 2 * nextLine && j < box7 + 2 * nextLine + across) ||
    (j > box7 + 3 * nextLine && j < box7 + 3 * nextLine + across)
  ) {
    return true;
  } else {
    return false;
  }
}

//isolate eight box in level three
function levelThreeGridBox8(j) {
  let box8 = 20;
  let across = 2;
  let nextLine = 12;
  if (
    (j > box8 && j < box8 + across) ||
    (j > box8 + nextLine && j < box8 + nextLine + across) ||
    (j > box8 + 2 * nextLine && j < box8 + 2 * nextLine + across) ||
    (j > box8 + 3 * nextLine && j < box8 + 3 * nextLine + across)
  ) {
    return true;
  } else {
    return false;
  }
}

//isolate ninth box in level three
function levelThreeGridBox9(j) {
  let box9 = 61;
  let across = 2;
  let nextLine = 12;
  if (
    (j > box9 && j < box9 + across) ||
    (j > box9 + nextLine && j < box9 + nextLine + across) ||
    (j > box9 + 2 * nextLine && j < box9 + 2 * nextLine + across) ||
    (j > box9 + 3 * nextLine && j < box9 + 3 * nextLine + across)
  ) {
    return true;
  } else {
    return false;
  }
}

//isolate tenth box in level three
function levelThreeGridBox10(j) {
  let box10 = 62;
  let across = 2;
  let nextLine = 12;
  if (
    (j > box10 && j < box10 + across) ||
    (j > box10 + nextLine && j < box10 + nextLine + across) ||
    (j > box10 + 2 * nextLine && j < box10 + 2 * nextLine + across) ||
    (j > box10 + 3 * nextLine && j < box10 + 3 * nextLine + across)
  ) {
    return true;
  } else {
    return false;
  }
}

//isolate eleventh box in level three
function levelThreeGridBox11(j) {
  let box11 = 63;
  let across = 2;
  let nextLine = 12;
  if (
    (j > box11 && j < box11 + across) ||
    (j > box11 + nextLine && j < box11 + nextLine + across) ||
    (j > box11 + 2 * nextLine && j < box11 + 2 * nextLine + across) ||
    (j > box11 + 3 * nextLine && j < box11 + 3 * nextLine + across)
  ) {
    return true;
  } else {
    return false;
  }
}

//isolate twelvth box in level three
function levelThreeGridBox12(j) {
  let box12 = 64;
  let across = 2;
  let nextLine = 12;
  if (
    (j > box12 && j < box12 + across) ||
    (j > box12 + nextLine && j < box12 + nextLine + across) ||
    (j > box12 + 2 * nextLine && j < box12 + 2 * nextLine + across) ||
    (j > box12 + 3 * nextLine && j < box12 + 3 * nextLine + across)
  ) {
    return true;
  } else {
    return false;
  }
}

//isolate thirteenth box in level three
function levelThreeGridBox13(j) {
  let box13 = 65;
  let across = 2;
  let nextLine = 12;
  if (
    (j > box13 && j < box13 + across) ||
    (j > box13 + nextLine && j < box13 + nextLine + across) ||
    (j > box13 + 2 * nextLine && j < box13 + 2 * nextLine + across) ||
    (j > box13 + 3 * nextLine && j < box13 + 3 * nextLine + across)
  ) {
    return true;
  } else {
    return false;
  }
}

//isolate fourteenth box in level three
function levelThreeGridBox14(j) {
  let box14 = 66;
  let across = 2;
  let nextLine = 12;
  if (
    (j > box14 && j < box14 + across) ||
    (j > box14 + nextLine && j < box14 + nextLine + across) ||
    (j > box14 + 2 * nextLine && j < box14 + 2 * nextLine + across) ||
    (j > box14 + 3 * nextLine && j < box14 + 3 * nextLine + across)
  ) {
    return true;
  } else {
    return false;
  }
}

//isolate fifteenth box in level three
function levelThreeGridBox15(j) {
  let box15 = 67;
  let across = 2;
  let nextLine = 12;
  if (
    (j > box15 && j < box15 + across) ||
    (j > box15 + nextLine && j < box15 + nextLine + across) ||
    (j > box15 + 2 * nextLine && j < box15 + 2 * nextLine + across) ||
    (j > box15 + 3 * nextLine && j < box15 + 3 * nextLine + across)
  ) {
    return true;
  } else {
    return false;
  }
}

//isolate sixteenth box in level three
function levelThreeGridBox16(j) {
  let box16 = 68;
  let across = 2;
  let nextLine = 12;
  if (
    (j > box16 && j < box16 + across) ||
    (j > box16 + nextLine && j < box16 + nextLine + across) ||
    (j > box16 + 2 * nextLine && j < box16 + 2 * nextLine + across) ||
    (j > box16 + 3 * nextLine && j < box16 + 3 * nextLine + across)
  ) {
    return true;
  } else {
    return false;
  }
}

/*---------LEVEL THREE FUNCTIONS END HERE------------*/

/*--------------------------------LEVEL THREE---------------------------------*/

$(".difficultySetting03").click(function () {
  choiceOne = false;
  choiceTwo = false;
  activeState = false;
  resetClasses(defaultGrid, "levelOne");
  resetClasses(defaultGrid, "levelTwo");
  resetClasses(defaultGrid, "levelFour");

  reset();

  //assign classes to each box
  for (let j = 0; j < defaultGrid.length; j++) {
    //SELECTION OF COLOURS

    //LEVEL THREE GRID - colours
    if (
      //LEVEL THREE GRID - colour 1
      levelThreeGridBox1(j) == true
    ) {
      defaultGrid[j].classList.add("levelThreeBox1");
    } else if (
      //LEVEL THREE GRID - colour 2
      levelThreeGridBox2(j) == true
    ) {
      defaultGrid[j].classList.add("levelThreeBox2");
    } else if (
      //LEVEL THREE - colour 3
      levelThreeGridBox3(j) == true
    ) {
      defaultGrid[j].classList.add("levelThreeBox3");
    } else if (
      //LEVEL THREE - colour 4
      levelThreeGridBox4(j) == true
    ) {
      defaultGrid[j].classList.add("levelThreeBox4");
    } else if (
      //LEVEL THREE - colour 5
      levelThreeGridBox5(j) == true
    ) {
      defaultGrid[j].classList.add("levelThreeBox5");
    } else if (
      //LEVEL THREE - colour 6
      levelThreeGridBox6(j) == true
    ) {
      defaultGrid[j].classList.add("levelThreeBox6");
    } else if (
      //LEVEL THREE - colour 7
      levelThreeGridBox7(j) == true
    ) {
      defaultGrid[j].classList.add("levelThreeBox7");
    } else if (
      //LEVEL THREE - colour 8
      levelThreeGridBox8(j) == true
    ) {
      defaultGrid[j].classList.add("levelThreeBox8");
    } else if (
      //LEVEL THREE - colour 9
      levelThreeGridBox9(j) == true
    ) {
      defaultGrid[j].classList.add("levelThreeBox9");
    } else if (
      //LEVEL THREE - colour 10
      levelThreeGridBox10(j) == true
    ) {
      defaultGrid[j].classList.add("levelThreeBox10");
    } else if (
      //LEVEL THREE - colour 11
      levelThreeGridBox11(j) == true
    ) {
      defaultGrid[j].classList.add("levelThreeBox11");
    } else if (
      //LEVEL THREE - colour 12
      levelThreeGridBox12(j) == true
    ) {
      defaultGrid[j].classList.add("levelThreeBox12");
    } else if (
      //LEVEL THREE - colour 13
      levelThreeGridBox13(j) == true
    ) {
      defaultGrid[j].classList.add("levelThreeBox13");
    } else if (
      //LEVEL THREE - colour 14
      levelThreeGridBox14(j) == true
    ) {
      defaultGrid[j].classList.add("levelThreeBox14");
    } else if (
      //LEVEL THREE - colour 15
      levelThreeGridBox15(j) == true
    ) {
      defaultGrid[j].classList.add("levelThreeBox15");
    } else if (
      //LEVEL THREE - colour 16
      levelThreeGridBox16(j) == true
    ) {
      defaultGrid[j].classList.add("levelThreeBox16");
    }
  }

  //ADDITIONAL STYLING FOR LEVEL THREE

  for (let j = 0; j < defaultGrid.length; j++) {
    let box1 = 13;
    let box2 = 14;
    let box3 = 16;
    let box4 = 18;
    let box5 = 61;
    let box6 = 63;
    let box7 = 65;
    let box8 = 67;
    let across = 2;
    let nextLine = 12;

    //LEVEL THREE - Borders
    if (
      // LEVEL THREE - Top border
      j > box1 - nextLine &&
      j < box4 + across - nextLine
    ) {
      if (j == box1 + 3 * nextLine) {
        defaultGrid[j].style.borderBottom = "1px solid black";
        defaultGrid[j].style.borderLeft = "1px solid black";
      }
      defaultGrid[j].style.borderBottom = "1px solid black";
    } else if (
      //LEVEL THREE - left border
      j == box1 ||
      j == box1 + nextLine ||
      j == box1 + 2 * nextLine ||
      j == box1 + 3 * nextLine ||
      j == box1 + 4 * nextLine ||
      j == box1 + 5 * nextLine ||
      j == box1 + 6 * nextLine ||
      j == box1 + 7 * nextLine
    ) {
      defaultGrid[j].style.borderRight = "1px solid black";
    } else if (
      //LEVEL THREE - first vertical border
      j == box2 ||
      j == box2 + nextLine ||
      j == box2 + 2 * nextLine ||
      j == box2 + 3 * nextLine ||
      j == box2 + 4 * nextLine ||
      j == box2 + 5 * nextLine ||
      j == box2 + 6 * nextLine ||
      j == box2 + 7 * nextLine
    ) {
      if (j == box2 + 3 * nextLine) {
        defaultGrid[j].style.borderRight = "1px solid black";
        defaultGrid[j].style.borderBottom = "1px solid black";
        defaultGrid[j].style.backgroundColor = gridBackColour;
      } else {
        defaultGrid[j].style.borderRight = "1px solid black";

        defaultGrid[j].style.backgroundColor = gridBackColour;
      }
    } else if (
      //LEVEL THREE - second vertical border
      j == box3 ||
      j == box3 + nextLine ||
      j == box3 + 2 * nextLine ||
      j == box3 + 3 * nextLine ||
      j == box3 + 4 * nextLine ||
      j == box3 + 5 * nextLine ||
      j == box3 + 6 * nextLine ||
      j == box3 + 7 * nextLine
    ) {
      if (j == box3 + 3 * nextLine) {
        defaultGrid[j].style.borderRight = "1px solid black";
        defaultGrid[j].style.borderLeft = "1px solid black";
        defaultGrid[j].style.borderBottom = "1px solid black";
      } else {
        defaultGrid[j].style.borderRight = "1px solid black";
        defaultGrid[j].style.borderLeft = "1px solid black";
        defaultGrid[j].style.backgroundColor = gridBackColour;
      }
    } else if (
      //LEVEL THREE - third vertical border
      j == box3 + 2 ||
      j == box3 + nextLine + 2 ||
      j == box3 + 2 * nextLine + 2 ||
      j == box3 + 3 * nextLine + 2 ||
      j == box3 + 4 * nextLine + 2 ||
      j == box3 + 5 * nextLine + 2 ||
      j == box3 + 6 * nextLine + 2 ||
      j == box3 + 7 * nextLine + 2
    ) {
      if (j == box3 + 3 * nextLine + 2) {
        defaultGrid[j].style.borderRight = "1px solid black";

        defaultGrid[j].style.borderBottom = "1px solid black";

        defaultGrid[j].style.borderBottom = "1px solid black";
        defaultGrid[j].style.backgroundColor = "blue";
      } else defaultGrid[j].style.borderRight = "1px solid black";
      defaultGrid[j].style.backgroundColor = gridBackColour;
    } else if (
      //LEVEL THREE - middle horizontal border
      j > box1 + 3 * nextLine &&
      j < box4 + across + 3 * nextLine
    ) {
      defaultGrid[j].style.borderBottom = "1px solid black";
      defaultGrid[j].style.backgroundColor = gridBackColour;
    } else if (
      //LEVEL THREE - last vertical border
      j == box4 + across ||
      j == box4 + across + nextLine ||
      j == box4 + across + 2 * nextLine ||
      j == box4 + across + 3 * nextLine ||
      j == box4 + across + 4 * nextLine ||
      j == box4 + across + 5 * nextLine ||
      j == box4 + across + 6 * nextLine ||
      j == box4 + across + 7 * nextLine
    ) {
      defaultGrid[j].style.borderLeft = "1px solid black";
      defaultGrid[j].style.borderRight = "1px solid black";
    } else if (
      //LEVEL THREE - bottom border
      j > box5 + 4 * nextLine &&
      j < box8 + across + 4 * nextLine
    ) {
      defaultGrid[j].style.borderTop = "1px solid black";
    } else if (
      //LEVEL THREE - back colour 1
      levelOneGridBox1(j) == true
    ) {
      defaultGrid[j].style.backgroundColor = gridBackColour;
    } else if (
      //LEVEL THREE - back colour 2
      levelOneGridBox2(j) == true
    ) {
      defaultGrid[j].style.backgroundColor = gridBackColour;
    } else if (
      //LEVEL THREE - back colour 3
      levelOneGridBox3(j) == true
    ) {
      defaultGrid[j].style.backgroundColor = gridBackColour;
    } else if (
      //LEVEL THREE - back colour 4
      levelOneGridBox4(j) == true
    ) {
      defaultGrid[j].style.backgroundColor = gridBackColour;
    } else {
      document
        .getElementById("darkMode")
        .addEventListener("click", switchToDark);
    }
  }

  //LEVEL THREE GRID STRUCTURE - AFTER PLAY BUTTON IS CLICKED

document.getElementById("play").disabled = false;

  // on click for play button - level three
  level = 3;
  $("#play").click(function () {
    if (level == 3) {
      for (let j = 0; j < defaultGrid.length; j++) {
        //SELECTION OF COLOURS

        //LEVEL THREE GRID - colours
        if (
          //LEVEL THREE GRID - colour 1
          levelThreeGridBox1(j) == true
        ) {
          defaultGrid[j].style.backgroundColor = colourArrayShuffleThree[0];
          defaultGrid[j].classList.add("boxes");
        } else if (
          //LEVEL THREE - colour 2
          levelThreeGridBox2(j) == true
        ) {
          defaultGrid[j].style.backgroundColor = colourArrayShuffleThree[1];
          defaultGrid[j].classList.add("boxes");
        } else if (
          //LEVEL THREE - colour 3
          levelThreeGridBox3(j) == true
        ) {
          defaultGrid[j].style.backgroundColor = colourArrayShuffleThree[2];
          defaultGrid[j].classList.add("boxes");
        } else if (
          //LEVEL THREE - colour 4
          levelThreeGridBox4(j) == true
        ) {
          defaultGrid[j].style.backgroundColor = colourArrayShuffleThree[3];
          defaultGrid[j].classList.add("boxes");
        } else if (
          //LEVEL THREE - colour 5
          levelThreeGridBox5(j) == true
        ) {
          defaultGrid[j].style.backgroundColor = colourArrayShuffleThree[4];
          defaultGrid[j].classList.add("boxes");
        } else if (
          //LEVEL THREE - colour 6
          levelThreeGridBox6(j) == true
        ) {
          defaultGrid[j].style.backgroundColor = colourArrayShuffleThree[5];
          defaultGrid[j].classList.add("boxes");
        } else if (
          //LEVEL THREE - colour 7
          levelThreeGridBox7(j) == true
        ) {
          defaultGrid[j].style.backgroundColor = colourArrayShuffleThree[6];
          defaultGrid[j].classList.add("boxes");
        } else if (
          //LEVEL THREE - colour 8
          levelThreeGridBox8(j) == true
        ) {
          defaultGrid[j].style.backgroundColor = colourArrayShuffleThree[7];
          defaultGrid[j].classList.add("boxes");
        } else if (
          //LEVEL THREE - colour 9
          levelThreeGridBox9(j) == true
        ) {
          defaultGrid[j].style.backgroundColor = colourArrayShuffleThree[8];
          defaultGrid[j].classList.add("boxes");
        } else if (
          //LEVEL THREE - colour 10
          levelThreeGridBox10(j) == true
        ) {
          defaultGrid[j].style.backgroundColor = colourArrayShuffleThree[9];
          defaultGrid[j].classList.add("boxes");
        } else if (
          //LEVEL THREE - colour 11
          levelThreeGridBox11(j) == true
        ) {
          defaultGrid[j].style.backgroundColor = colourArrayShuffleThree[10];
          defaultGrid[j].classList.add("boxes");
        } else if (
          //LEVEL THREE - colour 12
          levelThreeGridBox12(j) == true
        ) {
          defaultGrid[j].style.backgroundColor = colourArrayShuffleThree[11];
          defaultGrid[j].classList.add("boxes");
        } else if (
          //LEVEL THREE - colour 13
          levelThreeGridBox13(j) == true
        ) {
          defaultGrid[j].style.backgroundColor = colourArrayShuffleThree[12];
          defaultGrid[j].classList.add("boxes");
        } else if (
          //LEVEL THREE - colour 14
          levelThreeGridBox14(j) == true
        ) {
          defaultGrid[j].style.backgroundColor = colourArrayShuffleThree[13];
          defaultGrid[j].classList.add("boxes");
        } else if (
          //LEVEL THREE - colour 15
          levelThreeGridBox15(j) == true
        ) {
          defaultGrid[j].style.backgroundColor = colourArrayShuffleThree[14];
          defaultGrid[j].classList.add("boxes");
        } else if (
          //LEVEL THREE - colour 16
          levelThreeGridBox16(j) == true
        ) {
          defaultGrid[j].style.backgroundColor = colourArrayShuffleThree[15];
          defaultGrid[j].classList.add("boxes");
        }
        //flip to backside after interval
        setTimeout(flipToBack, 1000);
      }

      if (!activeState) {
        activeState = true;
      }
      document.getElementById("play").disabled = true;
      updateNumOfClicks("3");
    document.getElementById("numOfClicksLeft").innerHTML = numOfClicksLeft;
    }
  });

  $(".levelThreeBox1").click(function () {
    if (activeState) {
      checkColour("box1", "levelThreeBox1", levelThreeObj);

      let boxes = $(".boxes");
      let counter = 0;

      for (let i = 0; i < boxes.length; i++) {
        if (boxes[i].style.backgroundColor != gridBackColour) {
          counter++;
        }
      }

      if (counter == boxes.length) {
        setTimeout(function () {
          levelFour();
        }, 1000);
      }
    }
  });

  $(".levelThreeBox2").click(function () {
    if (activeState) {
      checkColour("box2", "levelThreeBox2", levelThreeObj);

      let boxes = $(".boxes");
      let counter = 0;

      for (let i = 0; i < boxes.length; i++) {
        if (boxes[i].style.backgroundColor != gridBackColour) {
          counter++;
        }
      }

      if (counter == boxes.length) {
        setTimeout(function () {
          levelFour();
        }, 1000);
      }
    }
  });

  $(".levelThreeBox3").click(function () {
    if (activeState) {
      checkColour("box3", "levelThreeBox3", levelThreeObj);

       let boxes = $(".boxes");
      let counter = 0;

      for (let i = 0; i < boxes.length; i++) {
        if (boxes[i].style.backgroundColor != gridBackColour) {
          counter++;
        }
      }

      if (counter == boxes.length) {
        setTimeout(function () {
          levelFour();
        }, 1000);
      }
    }
  });

  $(".levelThreeBox4").click(function () {
    if (activeState) {
      checkColour("box4", "levelThreeBox4", levelThreeObj);

      let boxes = $(".boxes");
      let counter = 0;

      for (let i = 0; i < boxes.length; i++) {
        if (boxes[i].style.backgroundColor != gridBackColour) {
          counter++;
        }
      }

      if (counter == boxes.length) {
        setTimeout(function () {
          levelFour();
        }, 1000);
      }
    }
  });

  $(".levelThreeBox5").click(function () {
    if (activeState) {
      checkColour("box5", "levelThreeBox5", levelThreeObj);

      let boxes = $(".boxes");
      let counter = 0;

      for (let i = 0; i < boxes.length; i++) {
        if (boxes[i].style.backgroundColor != gridBackColour) {
          counter++;
        }
      }

      if (counter == boxes.length) {
        setTimeout(function () {
          levelFour();
        }, 1000);
      }
    }
  });

  $(".levelThreeBox6").click(function () {
    if (activeState) {
      checkColour("box6", "levelThreeBox6", levelThreeObj);

      let boxes = $(".boxes");
      let counter = 0;

      for (let i = 0; i < boxes.length; i++) {
        if (boxes[i].style.backgroundColor != gridBackColour) {
          counter++;
        }
      }

      if (counter == boxes.length) {
        setTimeout(function () {
          levelFour();
        }, 1000);
      }
    }
  });

  $(".levelThreeBox7").click(function () {
    if (activeState) {
      checkColour("box7", "levelThreeBox7", levelThreeObj);

      let boxes = $(".boxes");
      let counter = 0;

      for (let i = 0; i < boxes.length; i++) {
        if (boxes[i].style.backgroundColor != gridBackColour) {
          counter++;
        }
      }

      if (counter == boxes.length) {
        setTimeout(function () {
          levelFour();
        }, 1000);
      }
    }
  });

  $(".levelThreeBox8").click(function () {
    if (activeState) {
      checkColour("box8", "levelThreeBox8", levelThreeObj);

      let boxes = $(".boxes");
      let counter = 0;

      for (let i = 0; i < boxes.length; i++) {
        if (boxes[i].style.backgroundColor != gridBackColour) {
          counter++;
        }
      }

      if (counter == boxes.length) {
        setTimeout(function () {
          levelFour();
        }, 1000);
      }
    }
  });

  $(".levelThreeBox9").click(function () {
    if (activeState) {
      checkColour("box9", "levelThreeBox9", levelThreeObj);

      let boxes = $(".boxes");
      let counter = 0;

      for (let i = 0; i < boxes.length; i++) {
        if (boxes[i].style.backgroundColor != gridBackColour) {
          counter++;
        }
      }

      if (counter == boxes.length) {
        setTimeout(function () {
          levelFour();
        }, 1000);
      }
    }
  });

  $(".levelThreeBox10").click(function () {
    if (activeState) {
      checkColour("box10", "levelThreeBox10", levelThreeObj);

      let boxes = $(".boxes");
      let counter = 0;

      for (let i = 0; i < boxes.length; i++) {
        if (boxes[i].style.backgroundColor != gridBackColour) {
          counter++;
        }
      }

      if (counter == boxes.length) {
        setTimeout(function () {
          levelFour();
        }, 1000);
      }
    }
  });

  $(".levelThreeBox11").click(function () {
    if (activeState) {
      checkColour("box11", "levelThreeBox11", levelThreeObj);

      let boxes = $(".boxes");
      let counter = 0;

      for (let i = 0; i < boxes.length; i++) {
        if (boxes[i].style.backgroundColor != gridBackColour) {
          counter++;
        }
      }

      if (counter == boxes.length) {
        setTimeout(function () {
          levelFour();
        }, 1000);
      }
    }
  });

  $(".levelThreeBox12").click(function () {
    if (activeState) {
      checkColour("box12", "levelThreeBox12", levelThreeObj);

      let boxes = $(".boxes");
      let counter = 0;

      for (let i = 0; i < boxes.length; i++) {
        if (boxes[i].style.backgroundColor != gridBackColour) {
          counter++;
        }
      }

      if (counter == boxes.length) {
        setTimeout(function () {
          levelFour();
        }, 1000);
      }
    }
  });

  $(".levelThreeBox13").click(function () {
    if (activeState) {
      checkColour("box13", "levelThreeBox13", levelThreeObj);

      let boxes = $(".boxes");
      let counter = 0;

      for (let i = 0; i < boxes.length; i++) {
        if (boxes[i].style.backgroundColor != gridBackColour) {
          counter++;
        }
      }

      if (counter == boxes.length) {
        setTimeout(function () {
          levelFour();
        }, 1000);
      }
    }
  });

  $(".levelThreeBox14").click(function () {
    if (activeState) {
      checkColour("box14", "levelThreeBox14", levelThreeObj);

      let boxes = $(".boxes");
      let counter = 0;

      for (let i = 0; i < boxes.length; i++) {
        if (boxes[i].style.backgroundColor != gridBackColour) {
          counter++;
        }
      }

      if (counter == boxes.length) {
        setTimeout(function () {
          levelFour();
        }, 1000);
      }
    }
  });

  $(".levelThreeBox15").click(function () {
    if (activeState) {
      checkColour("box15", "levelThreeBox15", levelThreeObj);

      let boxes = $(".boxes");
      let counter = 0;

      for (let i = 0; i < boxes.length; i++) {
        if (boxes[i].style.backgroundColor != gridBackColour) {
          counter++;
        }
      }

      if (counter == boxes.length) {
        setTimeout(function () {
          levelFour();
        }, 1000);
      }
    }
  });

  $(".levelThreeBox16").click(function () {
    if (activeState) {
      checkColour("box16", "levelThreeBox16", levelThreeObj);

      let boxes = $(".boxes");
      let counter = 0;

      for (let i = 0; i < boxes.length; i++) {
        if (boxes[i].style.backgroundColor != gridBackColour) {
          counter++;
        }
      }

      if (counter == boxes.length) {
        setTimeout(function () {
          levelFour();
        }, 1000);
      }
    }
  });
});
